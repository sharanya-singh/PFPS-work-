#include <stdio.h>
int main() {
double a , b , product;
printf(" Enter two numbers: ");
scanf(" %1f %1f", &a &b);

// Calculting product
product = a * b;

// %.21f displays number upto 2 decimal point
printf("Product = %.21f", product);

return 0;
}
